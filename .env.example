# Proxmox Manager Portal - Environment Configuration (OPTIONAL)
#
# The application works out of the box without this file!
# This file is only needed if you want to customize settings or use in production.
#
# Quick Start (No .env needed):
#   docker compose up -d
#
# Production Setup:
#   1. Copy this file: cp .env.example .env
#   2. Generate secure secrets: openssl rand -base64 32
#   3. Update the values below
#   4. Run: docker compose up -d

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================
POSTGRES_DB=proxmox_manager
POSTGRES_USER=proxmox
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD

# ============================================================================
# JWT Authentication Secrets
# IMPORTANT: Generate secure random strings for production!
# Generate with: openssl rand -base64 32
# ============================================================================
JWT_SECRET=CHANGE_THIS_SECRET_USE_OPENSSL_RAND_BASE64_32
JWT_REFRESH_SECRET=CHANGE_THIS_REFRESH_SECRET_USE_OPENSSL_RAND_BASE64_32

# ============================================================================
# Application Configuration
# ============================================================================
# Port for the frontend (default: 8080)
PORT=8080

# CORS Configuration - restrict in production
ALLOWED_ORIGINS=http://localhost:8080
CORS_ORIGIN=http://localhost:8080

# Logging level (debug, info, warn, error)
LOG_LEVEL=info

# ============================================================================
# Advanced Configuration (usually not needed)
# ============================================================================
# Frontend configuration (handled by Docker internally)
# Only set these if running outside Docker or with custom networking
# VITE_API_URL=http://backend:3002
# VITE_PROXY_URL=http://proxy:3001
